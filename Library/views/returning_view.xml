<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>


        <record id="returing_action" model="ir.actions.act_window">
            <field name="name">Returing</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">returning</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="returning_menu" name="Giao dịch trả sách" parent="library_menu_root" sequence="5"/>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="returning_menu_1" name="Danh sách giao dịch" parent="returning_menu" action="returing_action" sequence="1"/>
    </data>
</odoo>